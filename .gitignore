# Local, client-side personalization — never commit (may hold firm-specific notes)
personalization.md

# Local-only Claude Code dev context (per-clone, agent-facing) — never commit
CLAUDE.local.md

# macOS / exFAT AppleDouble sidecar files
._*
.DS_Store
